# Turso Database Configuration
# 
# For Electron desktop app (recommended):
#   Leave VITE_TURSO_URL unset or use file:dax.db
#   The desktop app will use a local SQLite database automatically
#
# For browser development/testing (uses mock in-memory database):
#   Leave VITE_TURSO_URL unset or use file:dax.db
#   Data will NOT persist between page reloads
#
# For production with Turso Cloud (persistent remote database):
#   Set VITE_TURSO_URL to your Turso database URL
#   Set VITE_TURSO_AUTH_TOKEN to your authentication token
#
# Examples:
# VITE_TURSO_URL=file:dax.db                          # Local file (Electron only)
# VITE_TURSO_URL=libsql://your-database.turso.io     # Turso Cloud (browser compatible)
# VITE_TURSO_AUTH_TOKEN=your-auth-token-here         # Required for Turso Cloud

# Default: Use mock database for browser development
# VITE_TURSO_URL=file:dax.db

# Application Configuration
NODE_ENV=development
